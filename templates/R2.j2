hostname {{hostname}}
{% for interface in interfaces %}
interface {{interface.name}}
encapsulation dot1q {{interface.vlan_id}} native
ip address {{interface.ip}} {{interface.mask}}
no shut
exit
{% endfor %}